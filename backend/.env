# Copy this file to .env and fill in real values. Never commit .env itself.

# Flask session signing key — generate with:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=2c07579a4eb9c95b536378d74108adb2c9434af2e7a39c864d05a591a0195904

# AES-256 key for encrypting vault data at rest — generate with:
#   python -c "import os, base64; print(base64.b64encode(os.urandom(32)).decode())"
VAULT_ENCRYPTION_KEY=LUVok8RD/4IE78IDB4wZuoN86Tb162Cma6uZ4YJ1s7k=

# Database. Defaults to a local SQLite file if omitted.
# For MySQL: mysql+pymysql://username:password@localhost/ciphervault
DATABASE_URL=sqlite:///ciphervault.db

# Where the frontend is served from, for CORS.
FRONTEND_ORIGIN=http://localhost:5500

# Set to "production" once deployed behind HTTPS.
FLASK_ENV=development

# --- OTP email delivery (optional in development) ---
# Leave these blank to have OTP codes printed to the server console
# instead of emailed, so you can test locally without SMTP creds.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=rupayanchattaraj@gmail.com
SMTP_PASSWORD=rgjbmmmqylxpipks
SMTP_FROM=rupayanchattaraj@gmail.com
